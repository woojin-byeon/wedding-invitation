# 최종 런북 — 홈랩 → OCI 단일 k3s 클러스터 통합 (다운타임 허용)

확정 환경 기준 최종본. 목표: **홈랩에 있던 monitoring / AWX(24.6.1) / demo-targets 를
데이터 포함 그대로 다시 정상 기동** + OCI 2노드와 하나의 클러스터로 통합.

```
Single k3s (Flannel over Tailscale 100.x)
 ├─ OCI node1 (ARM) server  : 내장 Traefik, portfolio, cert-manager   [항상 가동]
 ├─ OCI node2 (ARM) agent   : 여유/향후 AWX 이전 대상                 [항상 가동]
 └─ Home VM  (amd64) agent  : monitoring, AWX, demo-targets           [가변]
```

## 치환값
```
TS_AUTHKEY   = Tailscale > Settings > Keys > 재사용 auth key
HOME_TS_IP   = 100.102.52.107
N1_TS_IP     = <node1: tailscale ip -4>
N2_TS_IP     = <node2: tailscale ip -4>
DUCKDNS_TOKEN= <기존과 동일>
```

---

## STEP 0 — 확정 완료 ✅

모든 버전이 확정되었습니다. 추가 확인 불필요.

| 컴포넌트 | 버전(확정) |
|----------|-----------|
| AWX Operator | **2.19.1** (`quay.io/ansible/awx-operator:2.19.1`) |
| AWX | 24.6.1 |
| kube-prometheus-stack | 88.3.0 |
| loki-stack | 2.10.3 |
| cert-manager / duckdns-webhook | v1.21.1 / v1.2.3 |

---

## STEP 1 — 백업 (홈랩, uninstall 전 필수)

```bash
chmod +x backup-homelab.sh
sudo ./backup-homelab.sh
scp ~/homelab-backup-*.tar.gz rocky@161.33.204.246:~/     # ★ 홈랩 밖으로
```
백업 tar 안에서 반드시 확인:
- `db/awx.sql` **비어있지 않음**
- `secrets/AWX__awx-secret-key.yaml` **존재** (없으면 중단하고 알려주세요)
- `helm/monitoring__monitoring-stack.values.yaml`, `helm/monitoring__loki.values.yaml`
- `manifests/awx-cr.yaml`, `manifests/demo-targets/*`
- `info/awx-operator-image.txt`

---

## STEP 2 — Tailscale (세 노드)

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --authkey "TS_AUTHKEY"
tailscale ip -4        # N1_TS_IP / N2_TS_IP / 100.102.52.107 확인
```

---

## STEP 3 — 기존 k3s 제거 (세 노드, tailscale·백업 tar 는 보존)

```bash
# OCI node1 : /usr/local/bin/k3s-uninstall.sh
# OCI node2 : /usr/local/bin/k3s-agent-uninstall.sh
# 홈랩 home : /usr/local/bin/k3s-uninstall.sh
```

---

## STEP 4 — Tailscale 평면으로 클러스터 재구성

### node1 (server)
```bash
curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="server \
  --node-ip=N1_TS_IP --node-external-ip=N1_TS_IP --advertise-address=N1_TS_IP \
  --flannel-iface=tailscale0 \
  --tls-san=161.33.204.246 --tls-san=N1_TS_IP \
  --write-kubeconfig-mode=644" sh -
sudo cat /var/lib/rancher/k3s/server/node-token      # 토큰
```
### node2 (agent)
```bash
curl -sfL https://get.k3s.io | K3S_URL=https://N1_TS_IP:6443 K3S_TOKEN='<토큰>' \
  INSTALL_K3S_EXEC="agent --node-ip=N2_TS_IP --node-external-ip=N2_TS_IP \
  --flannel-iface=tailscale0" sh -
```
### home (agent)
```bash
curl -sfL https://get.k3s.io | K3S_URL=https://N1_TS_IP:6443 K3S_TOKEN='<토큰>' \
  INSTALL_K3S_EXEC="agent --node-ip=100.102.52.107 --node-external-ip=100.102.52.107 \
  --flannel-iface=tailscale0" sh -
```
### 검증 (node1) — 크로스노드 통신이 핵심
```bash
kubectl get nodes -o wide         # 3개 Ready, INTERNAL-IP 전부 100.x
kubectl run t-home --image=busybox --overrides='{"spec":{"nodeName":"repository"}}' -- sleep 3600
kubectl run t-oci  --image=busybox -- sleep 3600
kubectl get pod -o wide
kubectl exec t-oci -- ping -c3 <t-home 의 podIP>     # 성공하면 flannel-over-tailscale OK
kubectl delete pod t-oci t-home
```

---

## STEP 5 — 라벨
```bash
kubectl label node <node1>    topology=oci  role=edge       --overwrite
kubectl label node <node2>    topology=oci  role=general    --overwrite
kubectl label node repository topology=home role=monitoring --overwrite
```

---

## STEP 6 — cert-manager + DuckDNS + 포트폴리오 (OCI)

k3s 내장 Traefik 사용(별도 설치 불필요). cert-manager 는 **홈랩과 동일 버전**으로:
```bash
helm repo add jetstack https://charts.jetstack.io
helm repo add cert-manager-webhook-duckdns https://prashantgupta24.github.io/cert-manager-webhook-duckdns/
helm repo update
helm install cert-manager jetstack/cert-manager -n cert-manager --create-namespace \
  --version v1.21.1 --set crds.enabled=true
helm install cert-manager-webhook-duckdns \
  cert-manager-webhook-duckdns/cert-manager-webhook-duckdns \
  -n cert-manager --version v1.2.3 --set duckdns.token='DUCKDNS_TOKEN'

# ClusterIssuer + 와일드카드 Certificate + 포트폴리오
kubectl apply -f portfolio-html.yaml
kubectl apply -f oci-portfolio-manifests.yaml    # ★ home-traefik/home-proxied/insecure-skip 3블록은 제외!
kubectl -n portfolio get certificate             # woojin-wildcard READY=True
```

---

## STEP 7 — 모니터링 재배포 (home 고정, values 재사용)

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update
B=$(ls -d ~/homelab-backup-*/ | tail -1)

# kube-prometheus-stack 88.3.0 (동일 버전)
cat > mon-override.yaml <<'EOF'
prometheus:
  prometheusSpec:
    nodeSelector: { topology: home }
grafana:
  nodeSelector: { topology: home }
prometheusOperator:
  nodeSelector: { topology: home }
kube-state-metrics:
  nodeSelector: { topology: home }
EOF
helm install monitoring-stack prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace --version 88.3.0 \
  -f "$B/helm/monitoring__monitoring-stack.values.yaml" -f mon-override.yaml

# loki-stack 2.10.3 (동일 버전, PVC 없음=ephemeral)
cat > loki-override.yaml <<'EOF'
loki:
  nodeSelector: { topology: home }
promtail:
  nodeSelector: { topology: home }
EOF
helm install loki grafana/loki-stack \
  -n monitoring --version 2.10.3 \
  -f "$B/helm/monitoring__loki.values.yaml" -f loki-override.yaml

# grafana/prometheus Ingress 복원 (host→Service)
kubectl apply -f "$B"/manifests/monitoring/*ingress*.yaml
```

---

## STEP 8 — AWX 24.6.1 재배포 + DB 복원 (핵심)

```bash
B=$(ls -d ~/homelab-backup-*/ | tail -1)
kubectl create namespace awx 2>/dev/null || true

# 8-1) ★ secret_key 를 CR 보다 먼저 (operator 가 새로 만들지 않도록)
kubectl -n awx apply -f "$B/secrets/AWX__awx-secret-key.yaml"
kubectl -n awx apply -f "$B/secrets/AWX__awx-admin-password.yaml" 2>/dev/null || true

# 8-2) operator 2.19.1 설치 (확정 버전)
#   방법 A: Helm (권장, 네임스페이스/이미지 자동)
helm repo add awx-operator https://ansible-community.github.io/awx-operator-helm/
helm repo update
helm install awx-operator awx-operator/awx-operator \
  -n awx --version 2.19.1 \
  --set nodeSelector.topology=home        # operator 도 home 노드에 고정
#   방법 B: kustomize (Helm 대신 쓸 경우)
#   kubectl apply -k "github.com/ansible/awx-operator/config/default?ref=2.19.1"
kubectl -n awx rollout status deploy/awx-operator-controller-manager

# 8-3) AWX CR 에 node_selector 추가 후 적용
#   spec:
#     node_selector: "topology=home"
kubectl apply -f "$B/manifests/awx-cr.yaml"
kubectl -n awx get pods -w     # postgres/web/task 가 repository(home) 에 뜰 때까지

# 8-4) DB 복원 (web/task 정지 → 로드 → 재기동)
kubectl -n awx scale deploy awx-task awx-web --replicas=0
PGUSER=$(kubectl -n awx get secret awx-postgres-configuration -o jsonpath='{.data.username}'|base64 -d)
PGPASS=$(kubectl -n awx get secret awx-postgres-configuration -o jsonpath='{.data.password}'|base64 -d)
PGDB=$(kubectl   -n awx get secret awx-postgres-configuration -o jsonpath='{.data.database}'|base64 -d)
kubectl -n awx exec -i awx-postgres-15-0 -- bash -lc \
  "PGPASSWORD='$PGPASS' psql -h 127.0.0.1 -U '$PGUSER' -d '$PGDB'" < "$B/db/awx.sql"
kubectl -n awx scale deploy awx-task awx-web --replicas=1
kubectl -n awx logs deploy/awx-task -c awx-task -f

# 8-5) AWX Ingress 복원
kubectl apply -f "$B"/manifests/awx/*ingress*.yaml
```
> secret_key 가 원본과 동일 → 복원된 DB 의 암호화 자격증명이 정상 복호화됨.

---

## STEP 9 — demo-targets 복원 + /api/status 원복

```bash
B=$(ls -d ~/homelab-backup-*/ | tail -1)
# gitea PVC 는 새로 생성됨 → 데이터 tar 복원
kubectl apply -f "$B"/manifests/demo-targets/*.yaml   # svc/deploy/pvc 등
# (gitea pod 기동 후)
GP=$(kubectl -n demo-targets get pod -l app=gitea -o name | head -1)
kubectl -n demo-targets exec -i "${GP#pod/}" -- tar xzf - -C /data < "$B/gitea/gitea-data.tar.gz"

# /api/status 를 in-cluster Prometheus 로 원복 (portfolio-nginx-conf 수정)
#   proxy_pass http://monitoring-stack-kube-prom-prometheus.monitoring.svc.cluster.local:9090/api/v1/query?query=up;
kubectl -n portfolio edit configmap portfolio-nginx-conf   # 아래 블록으로 교체
kubectl -n portfolio rollout restart deploy/portfolio-deploy
```
교체할 블록:
```nginx
location = /api/status {
  proxy_pass http://monitoring-stack-kube-prom-prometheus.monitoring.svc.cluster.local:9090/api/v1/query?query=up;
  proxy_set_header Host $host;
  proxy_connect_timeout 3s;
  proxy_read_timeout 3s;
  default_type application/json;
}
```

---

## STEP 10 — 검증

```bash
kubectl get nodes                 # 3 Ready
kubectl get pods -A -o wide       # monitoring/awx/demo-targets → repository(home)
curl -I https://woojin-world.duckdns.org
curl -I https://grafana.woojin-world.duckdns.org
curl -sI https://awx.woojin-world.duckdns.org      # 로그인 → 기존 자격증명 복호화 확인
```
노트북 OFF 테스트: `repository` NotReady + monitoring/awx Pending, 포트폴리오는 정상.

---

## 롤백 안전장치
- 최강 롤백 = **백업 tar 로 홈랩 단독 k3s 재구축**. 그래서 tar 를 홈랩 밖에 보관.
- AWX 는 `awx-secret-key` + `awx.sql` 만 있으면 어디서든 복원 가능.
```
