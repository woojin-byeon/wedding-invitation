확정했습니다. AWX Operator = 2.19.1 (AWX 24.6.1과 정확히 맞는 조합입니다). 이 값으로 STEP 8을 고정한 최종 확정판을 만들었습니다.

최종 확정판입니다.

**** — STEP 0을 "확정 완료" 표로 바꾸고, STEP 8-2를 Operator 2.19.1 Helm 설치(operator도 home 노드 고정)로 확정. 추가 확인 필요 없이 STEP 1부터 그대로 실행하시면 됩니다.
1.md
2.sh — 백업 스크립트(그대로 사용).
이제 실행 순서 요약 (버전 전부 고정됨)
STEP	내용	핵심1	홈랩 백업	awx.sql + awx-secret-key 확인, tar를 OCI로 scp
2	3노드 Tailscale up	100.x IP 기록
3	3노드 k3s 제거	tailscale·백업은 보존
4	Tailscale flannel로 재구성	busybox ping 테스트 통과가 관문
5	노드 라벨	home=repository
6	cert-manager v1.21.1 + 포트폴리오	프록시 3블록 제외
7	모니터링 88.3.0 / loki 2.10.3	values 재사용 + home 고정
8	AWX operator 2.19.1 + DB 복원	secret_key 먼저, 그다음 CR
9	demo-targets + /api/status 원복	in-cluster Prometheus로
10	검증	3 Ready + AWX 로그인
실행 직전 체크 2가지
gitea 데이터를 살릴 거면 백업 전에 잠깐 띄우세요 (지금 0/0):
Shell
1
kubectl -n demo-targets scale deploy gitea --replicas=1
2
# pod Running 확인 후 backup-homelab.sh 실행
3
 
더 많은 선 표시
AWX CR에 node_selector 추가 (STEP 8-3): 백업된 manifests/awx-cr.yaml을 열어 spec: 아래에 한 줄 넣고 apply하세요.
YAML
1
spec:
2
node_selector: "topology=home"
더 많은 선 표시

가장 위험한 관문은 딱 두 곳입니다: STEP 4의 크로스노드 ping(여기 통과하면 네트워크는 끝)과 STEP 8의 secret_key 선(先)적용(AWX 자격증명 복호화). 이 두 개만 지키면 나머지는 순조롭게 흘러갑니다.

진행하시다가 어느 STEP이든 막히면 그 지점의 명령어 출력/로그를 그대로 붙여주세요. 바로 잡아드리겠습니다. 그리고 이관이 끝나면 알려주세요 — 약속대로 **3노드 단일 클러스터(OCI 2 + home 1, Flannel-over-Tailscale)를 반영한 새 architecture.html**을 만들어 드리겠습니다.